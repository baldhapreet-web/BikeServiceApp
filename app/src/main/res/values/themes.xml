<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BikeServiceApp" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Main Brand Color -->
        <item name="colorPrimary">@color/bike_red</item>
        <item name="colorPrimaryContainer">@color/bike_red_dark</item>

        <!-- Button + Toolbar -->
        <item name="colorSecondary">@color/bike_red</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/bike_gray</item>

    </style>
</resources>